## **Unit 1 Practice Test – Shapes and Properties**

### 1.

What does `Circle(200, 150, 50)` do?

* A. Draws a square at (200, 150) with size 50
* B. Draws a circle with center at (200, 150) and radius 50
* C. Changes the canvas background to blue
* D. Draws a rectangle with width 200 and height 150

---

### 2.

Which of the following is a required parameter when drawing a circle?

* A. `fill`
* B. `centerX`
* C. `opacity`
* D. `border`

---

### 3.

How can you change the background color of the canvas?

* A. `Circle.fill = 'red'`
* B. `app.background = 'skyBlue'`
* C. `canvas.color = 'blue'`
* D. `Rect(0, 0, 400, 400, fill='blue')`

---

### 4.

Which of these is the correct way to draw a star with multiple colors?

```python
Star(200, 150, 50, 5, roundness=50, fill=...)
```

* A. `fill='red'`
* B. `fill=gradient('red', 'yellow')`
* C. `color='blue'`
* D. `opacity=100`

---

### 5.

Which property makes a shape partially transparent?

* A. `borderWidth`
* B. `opacity`
* C. `fill`
* D. `rotateAngle`

---

### 6.

What data type are the `size` and `font` properties for a Label?

* A. Boolean and Number
* B. Number and String
* C. String and Boolean
* D. Integer and List

---

### 7.

Which of the following Label properties require boolean values?

* A. `size` and `font`
* B. `bold` and `italic`
* C. `centerX` and `centerY`
* D. `rotateAngle` and `opacity`

---

### 8.

Which shapes can have a `border`?

* A. Circle, Oval, Rect
* B. Line and Label
* C. Only Labels
* D. Lines and Circles

---

### 9.

What does `Rect(100, 150, 200, 50, opacity=50)` do?

* A. Draws a rectangle that is fully opaque
* B. Draws a rectangle that is partially transparent
* C. Draws a rectangle with no color
* D. Draws a rectangle with dashed border

---

### 10.

Which property only affects the border and does **not** apply to labels?

* A. `opacity`
* B. `dashes`
* C. `rotateAngle`
* D. `fill`

---

### 11.

Which of the following is **false** about shapes?

* A. Shapes are drawn in layers; earlier shapes appear behind later shapes
* B. `centerX` and `centerY` set the vertical and horizontal positions of a shape
* C. Lines have a `border` property
* D. Colors must always be in quotes

---

### 12.

What does `None` mean when used as a property value?

* A. The default value is used
* B. The property is ignored
* C. The shape will automatically be deleted
* D. The shape will become a circle

---

### 13.

What is the special property used to make a line thicker?

* A. `lineWidth`
* B. `borderWidth`
* C. `opacity`
* D. `roundness`

---

### 14.

Which code draws an oval at (100, 140) with width 150 and height 250?

* A. `Oval(100, 140, 150, 250)`
* B. `Rect(100, 140, 150, 250)`
* C. `Circle(100, 140, 150, 250)`
* D. `Line(100, 140, 150, 250)`

---

### 15.

What is the correct way to rotate a shape?

* A. `rotate=45`
* B. `rotateAngle=45`
* C. `rotation=45`
* D. `angle=45`