# **Unit 1 Practice Test – Answer Key**

1. **B** – Draws a circle with center at (200, 150) and radius 50
2. **B** – `centerX` is required (along with `centerY` and `radius`)
3. **B** – `app.background = 'skyBlue'` changes the canvas background
4. **B** – `fill=gradient('red', 'yellow')` allows multiple colors
5. **B** – `opacity` controls transparency
6. **B** – `size` is a Number, `font` is a String
7. **B** – `bold` and `italic` are boolean properties
8. **A** – Circle, Oval, Rect can have a border
9. **B** – `opacity=50` makes the rectangle partially transparent
10. **B** – `dashes` only affects borders, not Labels
11. **C** – Lines **do not** have a `border` property
12. **B** – `None` means the property is ignored
13. **A** – `lineWidth` makes a line thicker
14. **A** – `Oval(100, 140, 150, 250)` draws an oval
15. **B** – `rotateAngle=45` rotates a shape

