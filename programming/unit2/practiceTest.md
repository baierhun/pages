## **Unit 2 Practice Test – Conditionals & Boolean Logic**

**1.** In Python, which operator means *equal to*?

* A. =
* B. ==
* C. !=
* D. =>

---

**2.** Which of the following is a Python logical operator?

* A. and
* B. %
* C. print()
* D. //

---

**3.** Write the word or phrase that completes this boolean expression. The expression checks if `score` is greater than 50 and less than 90.

```
score > 50 _____ score < 90
```

* A. or
* B. and
* C. not
* D. ==

---

**4.** An `elif` statement will run only if the previous `if` condition was false.

* A. True
* B. False

---

**5.** What will be the result of this Boolean expression if `x = 5`?

```
x > 10 or x < 10
```

* A. True
* B. False

---

<div class="break"></div>

**6.** Which expression will evaluate to **False** if variable `y` has the value `20`?

* A. if y > 10 and y < 30
* B. if y < 10 or y > 15
* C. if y == 20
* D. if y != 20

---

**7.** What will print when this code runs? (Choose all that apply.)

```
temp = 75
if temp >= 80:
    print("Hot")
elif temp >= 70:
    print("Warm")
elif temp >= 60:
    print("Cool")
else:
    print("Cold")
```

* A. Hot
* B. Warm
* C. Cool
* D. Cold

---

**8.** Rewrite this boolean expression to check if `num` is **not equal to 0**.

```
if num == 0:
```

* A. if num =! 0:
* B. if num != 0:
* C. if num not 0:
* D. if num <> 0:

---

<div class="break"></div>

**9.** Which of the following is the correct syntax for an `if` statement?

* A.

```
if x == 5
    print("Yes")
```

* B.

```
if x == 5:
    print("Yes")
```

* C.

```
if x == 5;
    print("Yes")
```

* D.

```
if x = 5:
    print("Yes")
```

---

**10.** What will print when this code runs? (Choose all that apply.)

```
temp = 85

if temp >= 80:
    print("Hot")

if temp >= 70:
    print("Warm")

if temp >= 60:
    print("Cool")
else:
    print("Cold")
```

* A. Hot
* B. Warm
* C. Cool
* D. Cold

---

## **Answer Key**

1. B
2. A
3. B
4. A
5. A
6. D
7. B
8. B
9. B
10. A, B, C
